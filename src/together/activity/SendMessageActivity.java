package together.activity; import android.app.Activity;import android.content.Context;import android.content.pm.ActivityInfo;import android.os.Bundle;import android.view.View;import android.view.View.OnClickListener;import android.widget.AdapterView;import android.widget.AdapterView.OnItemSelectedListener;import android.widget.ArrayAdapter;import android.widget.EditText;import android.widget.Spinner;import android.widget.TextView;public class SendMessageActivity extends Activity{		private Context context;	private TextView sendMessageText;	private EditText placeEditText;	private EditText timeEditText;	private EditText myEventEditText;		private Spinner mySpinner;//	private  eventsArray  ;	@Override	public void onCreate(Bundle savedInstanceState) {		super.onCreate(savedInstanceState);		setContentView(R.layout.send_message);				setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_PORTRAIT);		context = this;				initUI();	}		private void initUI() {		String[]eventsArray = { getString(R.string.event_drama), getString(R.string.event_eating), 					getString(R.string.event_film),getString(R.string.event_ktv)}; 		sendMessageText = (TextView) findViewById(R.id.sendMessageText);		placeEditText = (EditText) findViewById(R.id.place_edit);		timeEditText  = (EditText) findViewById(R.id.time_edit);		myEventEditText = (EditText) findViewById(R.id.my_event_edit);				mySpinner = (Spinner) findViewById(R.id.event_spinner);		ArrayAdapter<String> adapter = new ArrayAdapter<String>( this, android.R.layout.simple_spinner_item,eventsArray); 		adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item); 		mySpinner.setAdapter(adapter);		mySpinner.setOnItemSelectedListener(new SpinnerSelectedListener());  				sendMessageText.setOnClickListener(new ClickListener());	}		class ClickListener implements OnClickListener{		@Override		public void onClick(View v) {			// TODO Auto-generated method stub			String place = placeEditText.getText()+"";			String time = timeEditText.getText()+"";			String event = "";//eventsArray[mySpinner.getSelectedItemPosition()];			sendMsg(place,time,event);		}		private void sendMsg(String place, String time, String event) {					}			}		class SpinnerSelectedListener implements OnItemSelectedListener{  		          public void onItemSelected(AdapterView<?> arg0, View arg1, int arg2,                  long arg3) {           }            public void onNothingSelected(AdapterView<?> arg0) {          }      }  }